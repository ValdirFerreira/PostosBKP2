namespace Business.TrataDados
{
    public class TrataDadosDashBoardTwo
    {

        //public static List<GraficoColunas> TratarDadosGrafico(List<GraficoColunaRetornoProcedure> grafico)
        //{
        //    var retorno = new List<GraficoColunas>();


        //    if (grafico.Any())
        //    {
        //        foreach (var item in grafico.Select(a => new { a.Descricao }).Distinct().ToList())
        //        {

        //            var graficoModel = new GraficoColunas();

        //            //graficoModel.Descricao = item.Descricao;
        //            //graficoModel.Base = grafico.Where(a => a.Descricao.Equals(item.Descricao)).Sum(a => a.Base);
        //            //graficoModel.Detratores = grafico.Where(a => a.Descricao.Equals(item.Descricao)).Select(a => a.Detractores).FirstOrDefault();
        //            //graficoModel.Neutros = grafico.Where(a => a.Descricao.Equals(item.Descricao)).Select(a => a.Neutros).FirstOrDefault();
        //            //graficoModel.NPS = grafico.Where(a => a.Descricao.Equals(item.Descricao)).Select(a => a.NPS).FirstOrDefault();
        //            //graficoModel.Promotores = grafico.Where(a => a.Descricao.Equals(item.Descricao)).Select(a => a.Promotores).FirstOrDefault();

        //            //if (graficoModel.Base != 0 || graficoModel.Detratores != 0 || graficoModel.Neutros != 0 || graficoModel.NPS != 0 || graficoModel.Promotores != 0)
        //            //{
        //            //    retorno.Add(graficoModel);
        //            //}
        //        }
        //    }
        //    else
        //        return null;

        //    return retorno;
        //}

    }
}
